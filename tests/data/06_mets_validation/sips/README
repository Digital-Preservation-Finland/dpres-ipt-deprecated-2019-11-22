CSC_test001, CSC_test002 and CSC_test003 are valid SIPs signed with mycert.pem.

missing_file is same as CSC_test001, but with one file missing, and should
fail file existance verfication.

wrong_signature is CSC_test001 with one byte of varmiste.sig signature
modified, and should fail signature verification.

wrong_signed_data is CSC_test001 with mets.xml checksum modified in
varmiste.sig, and should fail signature verification.

old_sip is CSC_test001 with varmiste.sig signed with expired certificate
oldcert.pem, and should fail signature verification.
