test.zip and test-longname.zip are formed with standard tools.
test-notazip.zip is just plaintext file.

test-absolute.zip, test-broken*.zip, test-relative1.zip and
test-relative2.zip have been made from test.zip with a hex editor.
test-relative3 has been made from test-longname.zip

test-broken-content has the content of the test.txt changed from
"test" to "tett" without changing the CRC.

test-broken-header has the final byte of the ZIP magic numbers changed.

test-absolute has a filepath that points to /tmp.

test-relative* have paths including "..", that is the link to parent
directory.
